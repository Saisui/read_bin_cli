# 在 help 中显示版本和作者

## 修改文件

### 1. `rust/Cargo.toml`
- 添加 `authors = ["Saisui"]`

### 2. `rust/src/main.rs` - draw_help 函数
- 帮助标题改为：`=== read-bin v{version} by Saisui ===`
- 使用 `env!("CARGO_PKG_VERSION")` 获取版本

## 验证
- `cargo build --release` 编译通过
- 运行后按 `?` 查看帮助弹窗，确认显示版本和作者
